name: Beta Release

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  id-token: write
  contents: read

jobs:
  beta:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          registry-url: 'https://registry.npmjs.org'
      - uses: pnpm/action-setup@v4
      - run: pnpm install

      - name: Set beta version
        run: |
          CURRENT_VERSION=$(node -p "require('./packages/dataqueue/package.json').version")
          TIMESTAMP=$(date +%Y%m%d%H%M%S)
          BETA_VERSION="${CURRENT_VERSION}-beta.${TIMESTAMP}"
          echo "Publishing beta version: $BETA_VERSION"
          cd packages/dataqueue && npm version "$BETA_VERSION" --no-git-tag-version && cd ../..
          cd packages/react && npm version "$BETA_VERSION" --no-git-tag-version && cd ../..
          cd packages/dashboard && npm version "$BETA_VERSION" --no-git-tag-version && cd ../..

      - name: Build all packages
        run: pnpm --filter '@nicnocquee/*' run build

      - name: Publish beta
        run: |
          cd packages/dataqueue && npm publish --tag beta --provenance --access public && cd ../..
          cd packages/react && npm publish --tag beta --provenance --access public && cd ../..
          cd packages/dashboard && npm publish --tag beta --provenance --access public && cd ../..
