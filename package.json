{
  "name": "pg-job-queue-monorepo",
  "private": true,
  "devDependencies": {
    "pnpm": "^9.0.0",
    "turbo": "^1.13.0",
    "prettier": "^3.6.2"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "format": "prettier --write ."
  }
}
